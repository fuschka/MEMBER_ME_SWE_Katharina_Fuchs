/* Text here dont know if one or 2 stars pobs two */
//const myCardAssigner = new CardAssigner();
//myCardAssigner.makePairs();
$(document).ready(function () {
    const myGame = new Game(8, 4, "Highscore");
});



/* TO DO
card and back tag name shall be set calling game constructor, back-tag-name must be given to card assigner to display cards

source mapping: sources shall be given to the constructor via addSource() method of game which uses the SourceMap of myCA
addSource (srcUrl) {
    this.myCA.sourceMap.set(String?this.myCA.sourceMap.size, String("srcUrl");
    this.myCa.mapSource;
}
call function in main after new Game? does it work?

app name ändern und bild!!!!

CODE AUFRÄUMEN

documentation machen und generieren
git repository
lastenheft, use cases

*/